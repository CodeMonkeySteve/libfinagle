#METASOURCES = AUTO
library_includedir=$(includedir)/$(PACKAGE)-$(VERSION)/sigslot
library_include_HEADERS = sigslot.h

docdir ?= $(datadir)/doc/$(PACKAGE)
pdfdir = $(docdir)/sigslot
pdf_DATA = $(srcdir)/doc/*
EXTRA_DIST = $(pdf_DATA)
